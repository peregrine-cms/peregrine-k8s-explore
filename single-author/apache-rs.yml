apiVersion: apps/v1beta2
kind: ReplicaSet
metadata:
  name: apache
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
        - name: apache
          image: peregrinecms/apache
          ports:
          - name: http
            containerPort: 80
          env:
            - name: APACHE_PROXY_URL
              value: http://peregrine:8080/
            - name: APACHE_DOMAIN
              value: author.peregrine.cxm
